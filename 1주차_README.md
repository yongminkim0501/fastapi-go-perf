# 1주차 — 환경 구축과 기준선

> 목표: **측정을 시작할 수 있는 상태**를 만든다. 이 주차에는 성능 결론을 내지 않는다.

전체 3주 계획 중 1주차. 여기가 가장 오래 걸리고, 여기서 실수하면 2·3주차 데이터가 전부 오염된다.

---

## 완료 조건

아래 6개가 전부 체크되면 1주차 종료.

- [ ] 게이트웨이 엔드포인트 4개가 동작한다
- [ ] Go 서비스가 정수 루프 / SHA-256 두 워크로드를 제공한다
- [ ] gRPC 채널이 재사용된다 (요청마다 재생성하지 않음)
- [ ] 양쪽 컨테이너에 동일한 CPU 제한이 걸려 있다
- [ ] Grafana 판독용 대시보드에 패널 4개가 떠 있다
- [ ] 캘리브레이션 표가 채워져 있다

---

## 실험 대상 (1단계 범위)

| 축 | 수준 |
|---|---|
| 워크로드 | 정수 루프 / SHA-256 |
| 동시성 모델 | `def` / `async def` |
| 처리 경로 | Python 내부 / Go gRPC 위임 |

**고정값**: CPU 2코어 · 워커 1개 · CPython 3.14

→ 2 × 2 × 2 = **8개 구성**

### 이번 범위에서 제외 (향후 확장)

NumPy · JSON 직렬화 워크로드, free-threaded 3.14t, ProcessPoolExecutor,
CPU 코어 수 축, 워커 수 축, perf 저수준 측정,
I/O·커넥션 풀·큐 병목 실험, Go 직접 호출 베이스라인

---

## 작업 순서

### 1. gRPC 채널 재사용 수정 ⚠️ 최우선

**이것부터 한다.** 레퍼런스 구현은 요청마다 `grpc.insecure_channel()`을 새로 생성하고 닫는다.
매 요청에 TCP 연결 + HTTP/2 핸드셰이크 비용이 붙어 위임 경로가 부당하게 불리하게 측정된다.

- [ ] 채널과 stub을 모듈 레벨에서 1회 생성하고 재사용
- [ ] async 경로용 `grpc.aio` 클라이언트 별도 준비
- [ ] 수정 전/후를 각 1회 측정해 개선폭 기록 → **README 소재로 사용**

### 2. FastAPI 게이트웨이 — 엔드포인트 4개

| 경로 | 핸들러 | 내부 동작 |
|---|---|---|
| `/sync/internal` | `def` | Python 연산 직접 수행 |
| `/async/internal` | `async def` | Python 연산 직접 수행 (이벤트 루프 블로킹 확인용) |
| `/sync/delegate` | `def` | 동기 gRPC로 Go 호출 |
| `/async/delegate` | `async def` | `grpc.aio`로 Go 호출 |

- [ ] 4개 모두 `complexity_level`과 `workload`(int_loop / sha256) 파라미터 수신
- [ ] 응답 body에 `server_time_ms` 포함 (게이트웨이 오버헤드 분리용)
- [ ] uvicorn 워커 수를 **명시적으로 1개 고정** (기본값에 의존하지 않는다)

### 3. Go 서비스 정비

- [ ] SHA-256 워크로드 함수 추가 (기존 정수 루프와 동일한 인터페이스)
- [ ] proto에 `workload` 필드 추가 후 재생성
- [ ] **연산 함수 내부의 `log.Printf` 제거** — 초당 수천 줄의 stdout I/O가 측정 노이즈로 유입된다

### 4. 자원 통제

- [ ] `docker-compose.yml`에서 게이트웨이·gocore 양쪽에 `cpus: "2.0"` 동일 적용
- [ ] `container_cpu_cfs_throttled_periods_total`이 수집되는지 확인 (제한에 걸리는지 봐야 함)
- [ ] 측정 중 불필요한 컨테이너 정지 (Grafana/InfluxDB는 측정 후 수집 권장)

### 5. Grafana 판독용 대시보드 — 패널 4개만

실험 중 화면에 띄워둘 대시보드. 그 외 지표는 Prometheus에 기록만 되고 보지 않는다.

- [ ] `http_reqs` — 실제 소화 처리량
- [ ] `dropped_iterations` — 포화의 실제 신호
- [ ] `http_req_duration` p95
- [ ] 컨테이너별 CPU 사용률 (gateway vs gocore)

> 진단용 지표(pprof, py-spy, GC pause 등)는 별도 대시보드로 분리하고 평소엔 닫아둔다.

### 6. 캘리브레이션 — `complexity_level`을 ms로 환산

RPS=1(동시성 없음)로 각 난이도를 30초씩 실행하고 `server_time_ms`의 중앙값을 기록한다.
**이 표가 있어야 Python과 Go를 같은 연산 시간에 놓고 비교할 수 있다.**

#### 정수 루프

| complexity | Python (ms) | Go (ms) | 비율 |
|---|---|---|---|
| 1 | | | |
| 5 | | | |
| 25 | | | |
| 100 | | | |
| 500 | | | |

#### SHA-256

| complexity | Python (ms) | Go (ms) | 비율 |
|---|---|---|---|
| 1 | | | |
| 5 | | | |
| 25 | | | |
| 100 | | | |
| 500 | | | |

- [ ] 두 표 모두 채움
- [ ] 2주차 측정에 사용할 **목표 연산 시간 3개 선정** (예: 5ms / 50ms / 200ms)와 그에 해당하는 난이도 확정

> 이 시점에 이미 결과 하나가 나온다 — 두 워크로드의 **비율 열이 얼마나 다른지**가 곧 GIL의 몫이다.

---

## 주의사항

**측정 순서를 지킨다.** 1번(채널 재사용)을 고치기 전에 캘리브레이션을 하면 전부 다시 해야 한다.

**캘리브레이션은 RPS=1로 한다.** 동시성이 들어가면 큐잉 지연이 섞여서 순수 연산 시간이 나오지 않는다.

**워밍업을 제외한다.** 첫 10초는 버리고 이후 구간의 중앙값을 쓴다.

**환경을 기록해둔다.** CPU 모델, 코어 수, Docker 버전, Python·Go 버전. 나중에 결과를 설명할 때 반드시 필요하다.

---

## 산출물

1주차가 끝나면 아래가 남아 있어야 한다.

- 동작하는 8개 구성의 실험 환경
- 채워진 캘리브레이션 표 2개
- gRPC 채널 수정 전/후 비교 수치
- 판독용 Grafana 대시보드
- 2주차에 사용할 난이도·RPS 범위 확정값

---

## 일정이 밀릴 경우

**워크로드를 정수 루프 하나로 줄인다.** 구성이 8개에서 4개로 줄고, 캘리브레이션 표도 하나만 채우면 된다.

이 경우 "GIL의 몫" 결론은 포기하지만, "async의 함정"과 "위임의 조건" 두 결론은 그대로 살아남는다.
축을 줄이는 것이 반복 횟수를 줄이는 것보다 낫다 — 반복이 부족하면 데이터 자체를 믿을 수 없게 된다.
